# Blog for ashsmith.io

## Requirements:

- Jekyll
- Gulp
- Sass

## Get running:

Get dependencies:

    npm install

Generate the CSS with:

    gulp

Run browser sync:

    gulp browser-sync

Run the site locally:

    jekyll serve --incremental
